package chap07.sec5.exam1;

public class B {
	
	public void method() {
		A a = new A();	//사용 가능
		a.field = "value";
		a.method();
	}
	
	//protected
	//A클래스의 protected 선언된 필드, 생성자, 메소드는 
	//동일한 패키지에 저장된 클래스들은 모두 접근해서 사용 가능

}
